#include <stdio.h>

/**
 * _strlen - returns the length of a string
 *@s: parameter to be used
 *
 *Return: 0
 */
int _strlen(char *s)
{
	int lenght = 0;

	while (*s != 0)
	{
		length++;
		s++;
	}
	return (0);
}
